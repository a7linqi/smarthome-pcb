# 智能家居项目开发日志

## 一、项目概述

### 1.1 项目目标
基于 STM32 + ESP32 + FreeRTOS + 巴法云，实现一个可通过手机远程控制的智能家居系统。

### 1.2 技术栈
- **主控芯片：** STM32F103C8T6（Cortex-M3, 72MHz）
- **WiFi模块：** ESP32-S3（ESP-IDF 开发）
- **操作系统：** FreeRTOS（实时多任务）
- **云平台：** 巴法云（MQTT 协议）
- **开发工具：** Keil MDK（STM32）+ VSCode + ESP-IDF（ESP32）

### 1.3 功能清单
| 功能 | 实现方式 |
|------|----------|
| 温湿度监测 | DHT11 传感器 |
| 光照检测 | 光敏电阻 + ADC |
| LED 灯控制 | 按键手动 + 光照自动 |
| RGB 彩灯 | 按键切换红绿蓝 |
| 门（舵机） | 按键开关 |
| 蜂鸣器报警 | 温度 >35°C 自动响 |
| OLED 显示 | 实时显示所有状态 |
| 手机远程控制 | ESP32 + 巴法云 MQTT |

---

## 二、硬件接线

### 2.1 STM32 引脚分配

```
PA0  → DHT11（温湿度传感器）
PA1  → 光敏电阻（ADC1通道1）
PA2  → KEY1（开关灯）
PA3  → KEY2（开关门）
PA4  → KEY3（RGB切换）
PA5  → KEY4（模式切换）
PA6  → 舵机SG90 PWM（TIM3通道1）
PA7  → 风扇电机 PWM（TIM3通道2）
PA9  → USART1_TX → ESP32 GPIO16(RX2)
PA10 → USART1_RX → ESP32 GPIO17(TX2)
PB5  → LED 白灯（高电平亮）
PB6  → RGB_R（低电平亮，共阳）
PB7  → RGB_G
PB8  → OLED SCL（I2C）
PB9  → OLED SDA（I2C）
PB12 → TB6612 AIN1（电机方向）
PB13 → TB6612 AIN2
PB14 → 蜂鸣器（高电平触发）
PB15 → RGB_B
```

### 2.2 ESP32 接线

```
ESP32 GPIO17 (TX2) → STM32 PA10 (RX)
ESP32 GPIO16 (RX2) → STM32 PA9  (TX)
GND 共地（必须！）
```

### 2.3 供电说明

| 设备 | 供电方式 | 注意事项 |
|------|----------|----------|
| STM32 | ST-Link 或 USB | — |
| ESP32 | USB 单独供电 | 不要从 STM32 取电 |
| 舵机 | 外部 5V | 必须外部供电，STM32 电流不够 |
| 电机 | TB6612 VM 接外部 5V | — |

---

## 三、STM32 开发过程

### 3.1 CubeMX 配置

**时钟配置：**
- HSE 8MHz × PLL×9 = 72MHz
- APB1 = 36MHz, APB2 = 72MHz

**GPIO 配置：**
- PA2~5: GPIO_Input, Pull-Up（按键）
- PB5: GPIO_Output（LED）
- PB6/7/15: GPIO_Output（RGB）
- PB14: GPIO_Output（蜂鸣器）

**ADC1 配置：**
- 通道0 (PA0): DHT11
- 通道1 (PA1): 光敏电阻
- 扫描模式，规则转换

**TIM3 配置：**
- 通道1 (PA6): PWM 50Hz（舵机）
- 通道2 (PA7): PWM 50Hz（风扇）

**I2C1 配置：**
- PB8 (SCL), PB9 (SDA)
- 硬件 I2C，从机地址 0x78

**USART1 配置（手动添加）：**
- PA9 (TX), PA10 (RX)
- 115200 波特率, 8N1
- 开启 NVIC 中断

**FreeRTOS 配置：**
- 堆大小改为 8192
- 使用 CMSIS-RTOS v1 API

### 3.2 硬件驱动开发

#### 3.2.1 LED 驱动（led.c/h）
```c
// PB5 高电平点亮
void LED_On(void)   { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_5, GPIO_PIN_SET); }
void LED_Off(void)  { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_5, GPIO_PIN_RESET); }
void LED_Toggle(void) { HAL_GPIO_TogglePin(GPIOB, GPIO_PIN_5); }

// RGB 低电平点亮（共阳极）
void RGB_Red(void)   { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_6, GPIO_PIN_RESET); }
void RGB_Green(void) { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_7, GPIO_PIN_RESET); }
void RGB_Blue(void)  { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_15, GPIO_PIN_RESET); }
void RGB_Off(void)   { /* 全部置高 */ }
```

#### 3.2.2 按键驱动（key.c/h）
```c
// 扫描4个按键，消抖+等松手
uint8_t Key_Scan(void) {
    if (HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_2) == GPIO_PIN_RESET) {
        HAL_Delay(20);                    // 消抖
        while (HAL_GPIO_ReadPin(...) == GPIO_PIN_RESET);  // 等松手
        return 1;  // KEY1
    }
    // KEY2~4 类似...
}
```

#### 3.2.3 DHT11 驱动（dht11.c/h）
- 单总线协议：主机拉低 18ms → 拉高等响应 → 读40位数据
- 返回值：0=成功, 1=无响应, 2=校验错误
- 需要外部 4.7K 上拉电阻

#### 3.2.4 光敏传感器（light.c/h）
```c
// ADC1通道1，读取光照强度 0-4095
uint16_t Light_Read(void) {
    HAL_ADC_Start(&hadc1);
    HAL_ADC_PollForConversion(&hadc1, 100);
    return HAL_ADC_GetValue(&hadc1);
}
```

#### 3.2.5 舵机驱动（servo.c/h）
```c
// TIM3通道1, PWM 50Hz
// 0.5ms = 0度, 2.5ms = 180度
void Door_Open(void)  { __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, 250); }  // 90度
void Door_Close(void) { __HAL_TIM_SET_COMPARE(&htim3, TIM_CHANNEL_1, 50); }   // 0度
```

#### 3.2.6 蜂鸣器驱动（buzzer.c/h）
```c
// PB14 高电平触发
void Buzzer_On(void)  { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_14, GPIO_PIN_SET); }
void Buzzer_Off(void) { HAL_GPIO_WritePin(GPIOB, GPIO_PIN_14, GPIO_PIN_RESET); }
```

#### 3.2.7 OLED 驱动（oled.c/h）
- 硬件 I2C，SSD1306 控制器
- 128×64 像素，4行×16字符
- 支持中文显示（字模索引 0~15）

### 3.3 FreeRTOS 多任务改造

#### 3.3.1 为什么要用 FreeRTOS

裸机 while(1) 的问题：
- 读 DHT11 要 2 秒，期间按键没响应
- OLED 刷新慢，会卡住传感器读取
- 无法同时处理多个任务

FreeRTOS 的好处：
- 每个任务独立运行，互不阻塞
- 互斥锁保护共享数据，避免冲突
- 优先级控制，紧急任务优先执行

#### 3.3.2 共享数据结构

```c
// app.h - 所有任务共享的数据
typedef struct {
    uint8_t  temp;    // 温度
    uint8_t  humi;    // 湿度
    uint16_t light;   // 光照 (0-4095)
    uint8_t  mode;    // 0=手动, 1=自动
    uint8_t  door;    // 0=关, 1=开
    uint8_t  led;     // 0=关, 1=开
} SmartHome_Data_t;

extern SmartHome_Data_t data_t;       // 全局共享数据
extern SemaphoreHandle_t dataMutex;   // 互斥锁
extern SemaphoreHandle_t keySem;      // 按键信号量
```

#### 3.3.3 任务架构

```
freertos.c - MX_FREERTOS_Init():
    1. 创建互斥锁 dataMutex
    2. 创建二值信号量 keySem
    3. 创建5个任务：

    ┌─────────────┬────────┬──────┬──────────────────────┐
    │ 任务名       │ 优先级 │ 栈   │ 职责                  │
    ├─────────────┼────────┼──────┼──────────────────────┤
    │ SensorTask  │ 2      │ 256  │ 读 DHT11 + 光敏       │
    │ ControlTask │ 2      │ 256  │ 按键扫描 + 模式切换    │
    │ DisplayTask │ 1      │ 256  │ OLED 刷新显示         │
    │ AlarmTask   │ 1      │ 128  │ 温度>35°C 蜂鸣器报警  │
    │ UartTask    │ 1      │ 256  │ 与 ESP32 串口通信     │
    └─────────────┴────────┴──────┴──────────────────────┘
```

#### 3.3.4 SensorTask（传感器任务）

```c
void SensorTask(void *params) {
    (void)params;
    while(1) {
        uint8_t temp, humi;
        uint16_t light;

        // 读温湿度
        if (DHT11_ReadData(&temp, &humi) == 0) {
            xSemaphoreTake(dataMutex, portMAX_DELAY);  // 上锁
            data_t.temp = temp;
            data_t.humi = humi;
            xSemaphoreGive(dataMutex);                  // 释放锁
        }

        // 读光照
        light = Light_Read();
        xSemaphoreTake(dataMutex, portMAX_DELAY);
        data_t.light = light;
        xSemaphoreGive(dataMutex);

        osDelay(2000);  // 每2秒读一次
    }
}
```

**关键点：**
- 读传感器不用加锁（硬件操作）
- 写入 data_t 必须加锁（多个任务会读写）
- 加锁时间尽量短

#### 3.3.5 ControlTask（控制任务）

```c
void ControlTask(void *params) {
    (void)params;
    uint8_t key;
    static uint8_t state = 1;  // RGB 状态机
    static int8_t dir = 1;

    while(1) {
        // 阻塞等待按键中断（GPIO EXTI → keySem）
        xSemaphoreTake(keySem, portMAX_DELAY);
        osDelay(20);      // 消抖
        key = Key_Scan();  // 读取按键值

        // KEY4: 切换手动/自动模式
        if (key == 4) {
            xSemaphoreTake(dataMutex, portMAX_DELAY);
            data_t.mode = !data_t.mode;
            xSemaphoreGive(dataMutex);
        }

        if (data_t.mode == 0) {  // 手动模式
            if (key == 1) { LED_Toggle(); data_t.led = !data_t.led; }
            if (key == 2) { Door_Toggle(); data_t.door = !data_t.door; }
        }
        else {  // 自动模式
            // KEY3: RGB 彩灯循环（红→绿→蓝）
            if (key == 3) {
                if (state >= 3) { state = 3; dir = -1; }
                if (state <= 1) { state = 1; dir = 1; }
                state += dir;
                switch (state) {
                    case 1: RGB_Red();   break;
                    case 2: RGB_Green(); break;
                    case 3: RGB_Blue();  break;
                }
            }

            // 光照自动控制
            if (data_t.light < 2000) {
                LED_On(); data_t.led = 1;
            } else if (data_t.light > 4000) {
                LED_Off(); data_t.led = 0;
            }

            // KEY2: 门还是手动
            if (key == 2) {
                Door_Toggle(); data_t.door = !data_t.door;
            }
        }
    }
}
```

**关键点：**
- 按键用中断+信号量，不是轮询（省CPU）
- GPIO 中断回调在 main.c 里：`xSemaphoreGiveFromISR(keySem)`
- 读写 data_t 都要加锁

#### 3.3.6 DisplayTask（显示任务）

```c
void DisplayTask(void *params) {
    (void)params;
    while(1) {
        // 1. 快速拷贝数据，立即释放锁
        xSemaphoreTake(dataMutex, portMAX_DELAY);
        SmartHome_Data_t d = data_t;  // 拷贝一份
        xSemaphoreGive(dataMutex);

        // 2. 用局部变量慢慢刷新 OLED（不持锁）
        OLED_Clear();
        OLED_ShowChinese(1, 1, 0);   // 温
        OLED_ShowChinese(1, 3, 1);   // 度
        OLED_ShowNum(1, 6, d.temp, 2);
        // ... 其他显示

        osDelay(500);  // 每500ms刷新
    }
}
```

**关键点：**
- 先拷贝再释放，避免持锁时间过长（I2C 通信慢）
- 用局部变量 d 而不是直接读 data_t

#### 3.3.7 AlarmTask（报警任务）

```c
void AlarmTask(void *params) {
    (void)params;
    uint8_t temp;
    while(1) {
        // 快速读温度，立即释放锁
        xSemaphoreTake(dataMutex, portMAX_DELAY);
        temp = data_t.temp;
        xSemaphoreGive(dataMutex);

        if (temp > 35)
            Buzzer_On();
        else
            Buzzer_Off();

        osDelay(1000);  // 1秒检查一次
    }
}
```

#### 3.3.8 UartTask（串口通信任务）

```c
// 中断接收缓冲区
static uint8_t uart_rx_byte;
static char uart_rx_line[64];
static volatile uint8_t uart_rx_index = 0;

// 启动中断接收
void UART_StartReceive(void) {
    HAL_UART_Receive_IT(&huart1, &uart_rx_byte, 1);
}

// 接收完成回调（中断里调用）
void HAL_UART_RxCpltCallback(UART_HandleTypeDef *huart) {
    if (huart->Instance == USART1) {
        if (uart_rx_byte == '\n') {
            // 遇到换行符，一帧结束
            uart_rx_line[uart_rx_index] = '\0';
            uart_rx_index = 0;

            // 解析指令
            if (strcmp(uart_rx_line, "LED:ON") == 0) {
                xSemaphoreTake(dataMutex, portMAX_DELAY);
                data_t.led = 1;
                xSemaphoreGive(dataMutex);
                LED_On();
            }
            // ... 其他指令
        } else {
            // 缓存字节
            uart_rx_line[uart_rx_index++] = uart_rx_byte;
        }
        // 继续接收下一个字节
        HAL_UART_Receive_IT(&huart1, &uart_rx_byte, 1);
    }
}

// 任务主体：周期性上报数据
void UartTask(void *params) {
    (void)params;
    char tx_buf[128];
    UART_StartReceive();

    while(1) {
        // 拷贝数据
        xSemaphoreTake(dataMutex, portMAX_DELAY);
        uint8_t temp = data_t.temp;
        uint16_t light = data_t.light;
        uint8_t led = data_t.led;
        uint8_t door = data_t.door;
        xSemaphoreGive(dataMutex);

        // 格式化上报字符串
        snprintf(tx_buf, sizeof(tx_buf),
                 "T:%d,L:%u,LED:%s,DOOR:%s\n",
                 temp, light,
                 led ? "ON" : "OFF",
                 door ? "ON" : "OFF");

        // 发送给 ESP32
        HAL_UART_Transmit(&huart1, (uint8_t *)tx_buf, strlen(tx_buf), 100);

        osDelay(3000);  // 每3秒上报
    }
}
```

**关键点：**
- 接收用中断（不阻塞任务）
- 发送用轮询（简单可靠）
- 中断里只做"收字节+解析"，不做耗时操作
- 指令解析在中断回调里，不创建额外任务

---

## 四、ESP32 开发过程

### 4.1 ESP-IDF 环境搭建

1. 安装 VSCode + ESP-IDF 插件
2. 配置工具链（Python + CMake + Ninja）
3. 创建项目：`idf.py create-project SmartHome_ESP32`
4. 配置目标芯片：`idf.py set-target esp32s3`

### 4.2 项目结构

```
SmartHome_ESP32/
├── main/
│   ├── main.c                 ← 入口，创建任务
│   ├── app/
│   │   ├── task_cloud.c/h     ← 云平台通信（MQTT + UART）
│   │   ├── task_sensor.c      ← ESP32本地传感器（未用）
│   │   ├── task_actuator.c    ← ESP32本地执行器（未用）
│   │   └── task_display.c     ← ESP32本地显示（未用）
│   ├── cloud/
│   │   ├── wifi_manager.c/h   ← WiFi 连接管理
│   │   ├── mqtt_client.c      ← MQTT 客户端（巴法云）
│   │   ├── mqtt_handler.h     ← MQTT 接口声明
│   │   └── mqtt_config.h      ← MQTT 配置（服务器地址、密钥）
│   └── driver/
│       ├── uart_bridge.c/h    ← UART 桥接（ESP32 ↔ STM32）
│       ├── led.c/h            ← 板载 LED
│       ├── buzzer.c/h         ← 蜂鸣器
│       ├── servo.c/h          ← 舵机
│       └── dht11.c/h          ← DHT11
├── CMakeLists.txt             ← 编译配置
└── sdkconfig                  ← ESP-IDF 配置
```

### 4.3 WiFi 连接

```c
// wifi_manager.c
#include "esp_wifi.h"
#include "esp_event.h"

void wifi_manager_init(void) {
    // 初始化网络接口
    esp_netif_init();
    esp_event_loop_create_default();
    esp_netif_create_default_wifi_sta();

    // 初始化 WiFi
    wifi_init_config_t cfg = WIFI_INIT_CONFIG_DEFAULT();
    esp_wifi_init(&cfg);
}

int wifi_manager_connect(const char *ssid, const char *password) {
    wifi_config_t wifi_cfg = {
        .sta = {
            .ssid = "Xiaomi14",
            .password = "qwer123456",
        },
    };
    esp_wifi_set_mode(WIFI_MODE_STA);
    esp_wifi_set_config(WIFI_IF_STA, &wifi_cfg);
    esp_wifi_start();
    esp_wifi_connect();

    // 等待连接成功（简化版）
    vTaskDelay(pdMS_TO_TICKS(5000));
    return 0;
}
```

**关键点：**
- STA 模式（作为客户端连接路由器）
- SSID 和密码硬编码（实际项目应该用 NVS 存储）
- 连接后获取 IP，才能访问互联网

### 4.4 MQTT 客户端（巴法云）

```c
// mqtt_client.c
#include "mqtt_client.h"  // ESP-IDF 自带的 MQTT 库

static esp_mqtt_client_handle_t client = NULL;

// MQTT 事件处理
static void mqtt_event_handler(void *handler_args, esp_event_base_t base,
                               int32_t event_id, void *event_data) {
    esp_mqtt_event_handle_t event = event_data;

    switch (event->event_id) {
    case MQTT_EVENT_CONNECTED:
        ESP_LOGI(TAG, "MQTT 连接成功");
        // 订阅主题
        esp_mqtt_client_subscribe(client, "smarthome_data", 0);
        break;

    case MQTT_EVENT_DATA:
        ESP_LOGI(TAG, "收到消息: %.*s", event->data_len, event->data);
        // 收到巴法云下发的指令 → 转发给 STM32
        char cmd[64];
        int len = event->data_len;
        if (len >= sizeof(cmd)) len = sizeof(cmd) - 1;
        memcpy(cmd, event->data, len);
        cmd[len] = '\0';
        uart_bridge_send_cmd(cmd);  // 通过 UART 发给 STM32
        break;

    case MQTT_EVENT_DISCONNECTED:
        ESP_LOGW(TAG, "MQTT 断开");
        break;
    }
}

void mqtt_client_init(void) {
    esp_mqtt_client_config_t mqtt_cfg = {
        .broker.address.uri = "mqtt://mqtt.bemfa.com:9501",
        .credentials.client_id = "你的私钥",
    };
    client = esp_mqtt_client_init(&mqtt_cfg);
    esp_mqtt_client_register_event(client, MQTT_EVENT_ANY,
                                   mqtt_event_handler, NULL);
}

int mqtt_client_publish(const char *topic, const char *payload) {
    esp_mqtt_client_publish(client, topic, payload, 0, 0, 0);
    return 0;
}
```

**关键点：**
- 巴法云 MQTT 服务器：`mqtt://mqtt.bemfa.com:9501`
- Client ID 就是你的私钥
- 收到消息后直接转发给 STM32（通过 UART）
- 发布消息用于上报 STM32 的传感器数据

### 4.5 UART 桥接

```c
// uart_bridge.c
#include "driver/uart.h"

#define UART_PORT    UART_NUM_2
#define UART_TX_PIN  GPIO_NUM_17
#define UART_RX_PIN  GPIO_NUM_16
#define UART_BAUD    115200

void uart_bridge_init(void) {
    uart_config_t uart_cfg = {
        .baud_rate = UART_BAUD,
        .data_bits = UART_DATA_8_BITS,
        .parity    = UART_PARITY_DISABLE,
        .stop_bits = UART_STOP_BITS_1,
        .flow_ctrl = UART_HW_FLOWCTRL_DISABLE,
    };
    uart_driver_install(UART_PORT, 1024, 1024, 0, NULL, 0);
    uart_param_config(UART_PORT, &uart_cfg);
    uart_set_pin(UART_PORT, UART_TX_PIN, UART_RX_PIN,
                 UART_PIN_NO_CHANGE, UART_PIN_NO_CHANGE);
}

// 发送指令给 STM32
int uart_bridge_send_cmd(const char *cmd) {
    char buf[64];
    snprintf(buf, sizeof(buf), "%s\n", cmd);
    uart_write_bytes(UART_PORT, buf, strlen(buf));
    return 0;
}

// 从 STM32 接收数据（非阻塞）
int uart_bridge_receive(char *buf, int buf_size) {
    int len = uart_read_bytes(UART_PORT, (uint8_t *)buf,
                              buf_size - 1, pdMS_TO_TICKS(100));
    if (len > 0) {
        buf[len] = '\0';
    }
    return len;
}
```

**关键点：**
- ESP32 用 UART2（UART0 是调试串口，UART1 被 SPI flash 占用）
- GPIO16/17 是 UART2 的默认引脚
- 接收超时 100ms，非阻塞

### 4.6 主任务（task_cloud.c）

```c
void task_cloud(void *pvParameters) {
    // 1. 初始化 UART
    uart_bridge_init();

    // 2. 连接 WiFi
    wifi_manager_init();
    wifi_manager_connect("Xiaomi14", "qwer123456");

    // 3. 连接巴法云 MQTT
    mqtt_client_init();
    mqtt_client_start();

    // 4. 主循环：UART 数据 → MQTT 上报
    char uart_buf[256];
    while (1) {
        int len = uart_bridge_receive(uart_buf, sizeof(uart_buf));
        if (len > 0) {
            // STM32 上报的数据，转发到巴法云
            mqtt_client_publish("smarthome_data", uart_buf);
        }
        vTaskDelay(pdMS_TO_TICKS(200));
    }
}
```

**关键点：**
- 一个任务搞定：WiFi + MQTT + UART 收发
- MQTT 收到指令 → 在回调里直接转发 UART（不需要额外任务）
- UART 收到数据 → 在任务循环里转发 MQTT

---

## 五、完整数据流

### 5.1 手机控制设备（下发）

```
手机微信 → 巴法云小程序 → 发送 "LED:ON"
    ↓
巴法云 MQTT 服务器
    ↓ MQTT 协议
ESP32 (mqtt_event_handler 收到消息)
    ↓ 调用 uart_bridge_send_cmd("LED:ON")
    ↓ UART 发送 "LED:ON\n"
STM32 (USART1 中断接收)
    ↓ HAL_UART_RxCpltCallback 解析
    ↓ data_t.led = 1; LED_On();
LED 点亮
```

### 5.2 上报传感器数据

```
STM32 (UartTask 每3秒执行)
    ↓ 读取 data_t.temp, data_t.light, data_t.led, data_t.door
    ↓ 格式化: "T:25,L:2048,LED:ON,DOOR:OFF\n"
    ↓ HAL_UART_Transmit 发给 ESP32
ESP32 (task_cloud 循环接收)
    ↓ uart_bridge_receive 收到数据
    ↓ mqtt_client_publish("smarthome_data", "T:25,L:2048,LED:ON,DOOR:OFF")
巴法云 MQTT 服务器
    ↓
手机微信 → 巴法云小程序 → 显示数据
```

---

## 六、遇到的问题和解决

### 6.1 DHT11 读取失败
**现象：** 返回错误码 1（无响应）
**原因：** 缺少 4.7K 上拉电阻
**解决：** PA0 和 3.3V 之间接 4.7K 电阻

### 6.2 舵机不转
**现象：** 代码没问题，舵机没反应
**原因：** STM32 电流不够，舵机需要外部 5V 供电
**解决：** 舵机红线接外部 5V，棕线接 GND，确保共地

### 6.3 FreeRTOS 多任务数据冲突
**现象：** OLED 显示乱码，偶尔死机
**原因：** 多个任务同时读写 data_t，没有加锁
**解决：** 用互斥锁 dataMutex 保护，读写都要加锁

### 6.4 按键不灵敏
**现象：** 按一次触发多次
**原因：** 轮询模式下消抖不够可靠
**解决：** 改用 GPIO 中断 + 信号量，阻塞等待

### 6.5 OLED 刷新卡顿
**现象：** 其他任务响应变慢
**原因：** DisplayTask 持锁时间太长（I2C 通信慢）
**解决：** 先拷贝数据到局部变量，释放锁后再刷新

---

## 七、面试要点总结

### 7.1 项目亮点
- FreeRTOS 5 个任务协调运行
- 互斥锁 + 信号量 + 中断的综合运用
- ESP32 + MQTT 物联网通信
- 完整的"传感器→MCU→云平台→手机"链路

### 7.2 可能被问的问题

**Q: 为什么用 FreeRTOS？**
A: 裸机 while(1) 无法同时处理传感器读取、按键响应、OLED 刷新、串口通信。FreeRTOS 让每个任务独立运行，互不阻塞。

**Q: 互斥锁和信号量的区别？**
A: 互斥锁用于保护共享资源（同一时间只有一个任务能访问），信号量用于任务间通知（一个任务等待，另一个任务释放）。互斥锁有优先级继承，信号量没有。

**Q: 为什么按键用中断+信号量而不是轮询？**
A: 轮询会占用 CPU，中断+信号量可以让任务阻塞等待，释放 CPU 给其他任务。

**Q: ESP32 和 STM32 怎么通信？**
A: 串口（UART），115200 波特率，TX 接 RX 交叉连接。STM32 用中断接收，ESP32 用非阻塞读取。

**Q: 数据冲突怎么解决？**
A: 用互斥锁保护共享数据结构 data_t。所有任务读写 data_t 前都要 xSemaphoreTake，用完后 xSemaphoreGive。

**Q: 为什么 DisplayTask 要先拷贝再释放锁？**
A: OLED 刷新通过 I2C 通信，比较慢（几十毫秒）。如果持锁期间刷新，其他任务会被阻塞。先拷贝到局部变量，释放锁后再慢慢刷新，不耽误别人。

---

## 八、代码文件清单

### STM32 侧
| 文件 | 说明 |
|------|------|
| Core/Src/main.c | 主函数，初始化外设，启动 FreeRTOS |
| Core/Src/freertos.c | 创建互斥锁、信号量、5个任务 |
| Core/Src/usart.c | USART1 初始化（115200, 8N1） |
| Core/Src/stm32f1xx_it.c | 中断处理（GPIO EXTI + USART1） |
| Core/Src/stm32f1xx_hal_msp.c | MSP 初始化（GPIO、NVIC 配置） |
| User/App/app.h | 共享数据结构 + 任务声明 |
| User/Tasks/Sensor_task.c | 传感器任务 |
| User/Tasks/Control_task.c | 控制任务 |
| User/Tasks/Display_task.c | 显示任务 |
| User/Tasks/Alarm_task.c | 报警任务 |
| User/Tasks/Uart_task.c | 串口通信任务 |
| User/BSP/*.c/h | 硬件驱动 |

### ESP32 侧
| 文件 | 说明 |
|------|------|
| main/main.c | 入口，创建 cloud 任务 |
| main/app/task_cloud.c | 云平台通信（WiFi + MQTT + UART） |
| main/cloud/wifi_manager.c | WiFi 连接管理 |
| main/cloud/mqtt_client.c | MQTT 客户端（巴法云） |
| main/driver/uart_bridge.c | UART 桥接（ESP32 ↔ STM32） |
| main/CMakeLists.txt | 编译配置 |

---

*日志完成时间：2026年8月23日*
*项目路径：D:\Project\SmartHome*
